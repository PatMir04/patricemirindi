# FAO Data Integration Configuration
# Copy this file to .env and fill in your actual database credentials

# Database Type - Choose one: postgresql, mysql, mongodb, sqlite
DB_TYPE=postgresql

# Database Connection Settings
DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_dashboard_database
DB_USER=your_database_username
DB_PASSWORD=your_database_password

# For SQLite (simplest option for testing)
# DB_TYPE=sqlite
# DB_NAME=fao_data.db

# Optional: API Rate Limiting
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=3600

# Development Settings
NODE_ENV=development
LOG_LEVEL=info

# Production Settings (when deploying)
# NODE_ENV=production
# DATABASE_URL=your_full_database_connection_string